1. Open the `toolbar.py` file in a code editor.
2. Locate the `FloatingToolbar` class and the `setup_toolbar` method.
3. Modify the capture button's color to red by changing the `button_active` color from `#007804` (blue) to `#FF0000` (red).
4. Implement a darker red hover effect for the button by setting it to `#CC0000`.
5. Change the initial color of the ALIVE logo to white (`#FFFFFF`).
6. Update the ALIVE logo's color to turn red (`#FF4444`) when the capturing process starts.
7. Ensure the logo returns to white after the capturing process finishes.
8. Configure the grid layout for proper vertical centering of the elements.
9. Add tooltips to the frame and the step icon to indicate "Steps captured".
10. Save the changes made to `toolbar.py`.
11. Restart the toolbar by running the command `python -m src.toolbar` in the terminal to see the changes.